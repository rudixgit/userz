name: Tron 🇧🇬
on:
  schedule:
    - cron: '*/5 9-23 * * *'
    - cron: '*/10 0-3 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
     - uses: actions/setup-node@v3
       with:
         node-version: 19
     - name: Checkout
       uses: actions/checkout@v3
     - name: Cache
       id: cache-npm
       uses: actions/cache@v3
       env:
         cache-name: cache-pupp
       with:
         path: ~/.npm
         key: ${{ runner.os }}-pupp-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
         restore-keys: |
           ${{ runner.os }}-pupp-${{ env.cache-name }}-
           ${{ runner.os }}-pupp-
           ${{ runner.os }}-
     - name: 😳 Build
       run: |
          url="https://db.kloun.lol/db/_design/company/_view/cronews?limit=100"
          response=$(curl -s "$url")
          length=${#response}
          if [[ $length -gt 200 ]]; then
            echo "Output length is greater than 50 characters"
            npm install puppeteer nano jsdom async sanitize-html
            cd ./.github/crons/trans
            node ./cron.mjs
          else
            echo "Output length is less than or equal to 50 characters"
          fi
     